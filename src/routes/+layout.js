// For this version of SvelteKit, we'll handle authentication client-side
// The server-side redirect will be handled by the client-side logic
export async function load() {
	return {
		// Authentication will be handled client-side
	};
}